# PL3
Για όλες τις ασκήσεις υπάρχουν τα αντίστοιχα Makefile.
Για να γίνει build-> εντολή make all.
Στην 3.1 και 3.2 υπάρχει η εντολη make run για να τρέξει με τις προκαθορισμένους παραμέτρους όπως είναι στο Makefile(χρηση machines στα Linux σχολής για χρήση εκτός δικτύου πρέπει να γίνουν αλλαγές στο Makefile) 
Προτεινόμενη εκτέλεση στα Linux της σχολής 